{
  "name": "mc-converter-dev",
  "private": true,
  "scripts": {
    "dev": "concurrently --names api,ui --prefix-colors cyan,magenta \"uvicorn web.backend.server:app --reload\" \"npm run dev --prefix web/frontend\"",
    "build": "npm install --prefix web/frontend && npm run build --prefix web/frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
